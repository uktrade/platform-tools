version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies...
      - pip install poetry
      - poetry install

  build:
    commands:
      - echo Running tests...
      - poetry run tox
