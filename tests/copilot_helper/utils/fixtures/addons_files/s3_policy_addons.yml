my-s3-policy:
  type: s3-policy
  readonly: true
  deletion_policy: Delete

  services:
    - "web"
    - "other"

  environments:
    dev:
      bucket_name: my-bucket-dev
      deletion_policy: Retain
