my-s3-bucket:
  type: s3
  readonly: true
  services:
    - "web"
  ennvironments: # Misspelled key
    development:
      bucket-name: my-bucket-dev

my-s3-bucket-with-an-object:
  type: s3
  readonly: true
  services: 33 # Should be a list
  environments:
    development:
      bucket-name: my-bucket-dev
  objects:
    - key: healthcheck.txt
      body: |
        HEALTHCHECK WORKS!
