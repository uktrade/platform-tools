environments:
  - name: dev
  - name: prod
    requires_approval: true

codebases:
  - name: application
    repository: organisation/repository
    branch: main
    services:
      - web
      - worker
    environments:
      - name: dev
      - name: staging
      - name: prod
        requires_approval: true
