# Generated by copilot-helper v0.1-TEST / 2023-08-22 16:00:00
# The manifest for the "test-backend-service" service.
# Read the full specification for the "Backend Service" type at:
#  https://aws.github.io/copilot-cli/docs/manifest/backend-service/

# Your service name will be used in naming your resources like log groups, ECS services, etc.
name: test-backend-service
type: Backend Service

# Your service does not allow any traffic.

# Configuration for your containers and service.
image:
  location: backend-service-image-repository
  # Your task should have a health check.
  # Celery example: https://github.com/uktrade/copilot-python?tab=readme-ov-file#celery-health-check
  healthcheck:
    command: [ "CMD-SHELL", "launcher bash -c 'echo \"Dummy health check - please replace\"'" ]
    interval: 10s
    timeout: 5s
    retries: 2
    start_period: 10s

cpu: 256       # Number of CPU units for the task.
memory: 512    # Amount of memory in MiB used by the task.
count: # See https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/#count
  range: 2-10
  cooldown:
    in: 120s
    out: 60s
  cpu_percentage: 50
exec: true     # Enable running commands in your container.

storage:
  readonly_fs: true       # Limit to read-only access to mounted root filesystems.

observability:
  tracing: awsxray

variables:                    # Pass environment variables as key value pairs.
  PORT: 8080                  # The bootstrap container requires a $PORT env var
  OTEL_PROPAGATORS: xray
  OTEL_PYTHON_ID_GENERATOR: xray
  OTEL_SERVICE_NAME: ${COPILOT_APPLICATION_NAME}-${COPILOT_ENVIRONMENT_NAME}-test-backend-service
  OTEL_METRICS_EXPORTER: console,otlp
  OTEL_TRACES_EXPORTER: console,otlp
  OTEL_TRACES_SAMPLER: traceidratio
  OTEL_TRACES_SAMPLER_ARG: "0.05"
  TEST_VAR: TEST_VAR_VALUE


secrets:                      # Pass secrets from AWS Systems Manager (SSM) Parameter Store.
  TEST_SECRET: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/TEST_SECRET_SOURCE


# You can override any of the values defined above by environment.
#environments:
#  test:
#    count: 2               # Number of tasks to run for the "test" environment.
#    deployment:            # The deployment strategy for the "test" environment.
#       rolling: 'recreate' # Stops existing tasks before new ones are started for faster deployments.
