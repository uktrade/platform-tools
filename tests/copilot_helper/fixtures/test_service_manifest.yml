# Generated by copilot-helper v0.1-TEST / 2023-08-22 16:00:00
# The manifest for the "test-service" service.
# Read the full specification for the "Load Balanced Web Service" type at:
#  https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/

# Your service name will be used in naming your resources like log groups, ECS services, etc.
name: test-service
type: Load Balanced Web Service

# Distribute traffic to your service.
http:
  # Requests to this path will be forwarded to your service.
  # To match all requests you can use the "/" path.
  path: '/'
  # You can specify a custom health check path. The default is "/".
  # healthcheck: '/'
  target_container: nginx
  healthcheck:
    path: '/'
    port: 8080
    success_codes: '200,301,302'
    healthy_threshold: 3
    unhealthy_threshold: 2
    grace_period: 180s

sidecars:
  nginx:
    port: 443
    image: public.ecr.aws/uktrade/nginx-reverse-proxy:latest
    variables:
      SERVER: localhost:8000

  ipfilter:
    port: 8000
    image: public.ecr.aws/uktrade/ip-filter:latest
    variables:
      PORT: 8000
      SERVER: localhost:8080
      APPCONFIG_PROFILES: ipfilter:default:default
      IPFILTER_ENABLED: True
      EMAIL: sre@digital.trade.gov.uk

  appconfig:
    port: 2772
    image: public.ecr.aws/aws-appconfig/aws-appconfig-agent:2.x
    essential: true
    variables:
      ROLE_ARN: arn:aws:iam::763451185160:role/AppConfigIpFilterRole

# Configuration for your containers and service.
image:
  location: not-a-url
    # Port exposed through your container to route traffic to it.
  port: 8080

cpu: 256       # Number of CPU units for the task.
memory: 512 # Amount of memory in MiB used by the task.
count: # See https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/#count
  range: 2-10
  cooldown:
    in: 120s
    out: 60s
  cpu_percentage: 50
exec: true     # Enable running commands in your container.
network:
  connect: true # Enable Service Connect for intra-environment traffic between services.
  vpc:
    placement: 'private'

# The application currently may not work if the file system is readonly, e.g...
# FileNotFoundError: [Errno 2] No usable temporary directory found in ['/tmp', '/var/tmp', '/usr/tmp', '/workspace']
# ...so you may need to comment out the two lines below...
storage:
  readonly_fs: true       # Limit to read-only access to mounted root filesystems.

observability:
  tracing: awsxray

# Optional fields for more advanced use-cases.
#
variables:                    # Pass environment variables as key value pairs.
  PORT: 8080
  OTEL_PROPAGATORS: xray
  OTEL_PYTHON_ID_GENERATOR: xray
  OTEL_SERVICE_NAME: ${COPILOT_APPLICATION_NAME}-${COPILOT_ENVIRONMENT_NAME}-test-service
  OTEL_METRICS_EXPORTER: console,otlp
  OTEL_TRACES_EXPORTER: console,otlp
  OTEL_TRACES_SAMPLER: traceidratio
  OTEL_TRACES_SAMPLER_ARG: "0.05"
  TEST_VAR:


secrets:                      # Pass secrets from AWS Systems Manager (SSM) Parameter Store.
  TEST_SECRET: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/TEST_SECRET
  TEST_SECRET_2: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/TEST_SECRET_2
  GOOD_SECRET: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/GOOD_SECRET
  AWS_BAD_SECRET: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AWS_BAD_SECRET


# You can override any of the values defined above by environment.
environments:
  production:
    http:
      alias: test-service.trad.gav.ik
    memory: 1024
    count: # For options see https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/#count
      range: 1-10
      cooldown:
        in: 30s
        out: 60s
      cpu_percentage: 70
      memory_percentage:
        value: 80
        cooldown:
          in: 80s
          out: 160s
      requests: 10000
      response_time: 2s
  test:
    http:
      alias: test-service.landan.cloudapps.digital
    count: 2 # For options see https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/#count
