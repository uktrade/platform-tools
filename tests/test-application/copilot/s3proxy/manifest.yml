# Generated by copilot-helper 0.1.26 / 2023-08-24 12:55:51
# The manifest for the "s3proxy" service.
# Read the full specification for the "Load Balanced Web Service" type at:
#  https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/

# Your service name will be used in naming your resources like log groups, ECS services, etc.
name: s3proxy
type: Load Balanced Web Service

# Distribute traffic to your service.
http:
  # Requests to this path will be forwarded to your service.
  # To match all requests you can use the "/" path.
  path: '/'
  # You can specify a custom health check path. The default is "/".
  # healthcheck: '/'
  target_container: nginx
  healthcheck:
    path: '/'
    port: 8080
    success_codes: '200,301,302'
    healthy_threshold: 3
    unhealthy_threshold: 2
    grace_period: 90s

sidecars:
  nginx:
    port: 443
    image: public.ecr.aws/uktrade/nginx-reverse-proxy:latest
    variables:
      SERVER: localhost:8000

  ipfilter:
    port: 8000
    image: public.ecr.aws/uktrade/ip-filter:latest
    variables:
      PORT: 8000
      SERVER: localhost:8080
      APPCONFIG_PROFILES: ipfilter:default:default
      IPFILTER_ENABLED: True

  appconfig:
    port: 2772
    image: public.ecr.aws/aws-appconfig/aws-appconfig-agent:2.x
    essential: true
    variables:
      ROLE_ARN: arn:aws:iam::763451185160:role/AppConfigIpFilterRole

# Configuration for your containers and service.
image:
  location: public.ecr.aws/uktrade/copilot-bootstrap:latest
    # Port exposed through your container to route traffic to it.
  port: 8080

cpu: 256       # Number of CPU units for the task.
memory: 512 # Amount of memory in MiB used by the task.
count: # See https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/#count
  range: 2-10
  cooldown:
    in: 120s
    out: 60s
  cpu_percentage: 50
exec: true     # Enable running commands in your container.
network:
  connect: true # Enable Service Connect for intra-environment traffic between services.

storage:
  readonly_fs: true       # Limit to read-only access to mounted root filesystems.

# Optional fields for more advanced use-cases.
#
variables:                    # Pass environment variables as key value pairs.
  PORT: 8080


secrets:                      # Pass secrets from AWS Systems Manager (SSM) Parameter Store.
  APP_ENV: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/APP_ENV
  AUTHBROKER_CLIENT_ID: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AUTHBROKER_CLIENT_ID
  AUTHBROKER_CLIENT_SECRET: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AUTHBROKER_CLIENT_SECRET
  AUTHBROKER_URL: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AUTHBROKER_URL
  AWS_ACCESS_KEY_ID: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AWS_ACCESS_KEY_ID
  AWS_DEFAULT_REGION: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AWS_DEFAULT_REGION
  AWS_S3_BUCKET: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AWS_S3_BUCKET
  AWS_S3_ENDPOINT: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AWS_S3_ENDPOINT
  AWS_S3_HEALTHCHECK_KEY: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AWS_S3_HEALTHCHECK_KEY
  AWS_S3_REGION: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AWS_S3_REGION
  AWS_SECRET_ACCESS_KEY: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AWS_SECRET_ACCESS_KEY
  AWS_STORAGE_BUCKET_NAME: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/AWS_STORAGE_BUCKET_NAME
  DISABLE_COLLECTSTATIC: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/DISABLE_COLLECTSTATIC
  DJANGO_SECRET_KEY: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/DJANGO_SECRET_KEY
  ELASTIC_APM_SECRET_TOKEN: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/ELASTIC_APM_SECRET_TOKEN
  ELASTIC_APM_SERVER_URL: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/ELASTIC_APM_SERVER_URL
  SSO_CLIENT_ID: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/SSO_CLIENT_ID
  SSO_CLIENT_SECRET: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/SSO_CLIENT_SECRET
  SSO_URL: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/SSO_URL


# You can override any of the values defined above by environment.
environments:
  dev:
    http:
      alias: test-application-s3proxy-dev.london.cloudapps.digital
  prod:
    http:
      alias: static.test-application.trade.gov.uk
  staging:
    http:
      alias: static.test-application.staging.uktrade.digital
