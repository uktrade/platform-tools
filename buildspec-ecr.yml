version: 0.2

phases:
  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
      - aws ecr-public get-login-password --region us-east-1 
      - aws ecr-public get-login-password --region us-east-1 | docker login --username AWS --password-stdin public.ecr.aws/uktrade
  build:
    commands:
      - cd images/nginx-dbt-platform
      - echo Build started on `date`
      - echo Building the Docker image...
      - docker build -t nginx-dbt-platform:ch-latest .
      - docker tag nginx-dbt-platform:ch-latest public.ecr.aws/uktrade/nginx-dbt-platform:ch-latest
  post_build:
    commands:
      - echo Build completed on `date`
      - echo Pushing the Docker image...
      - docker push public.ecr.aws/uktrade/nginx-dbt-platform:ch-latest