version: 0.2

phases:
  build:
    commands:
    - >
        if [ -f ./custom-build/post-deploy.sh ]; then 
          bash ./custom-build/post-deploy.sh; 
        fi

  post_build:
    commands:
    - echo "Done"
