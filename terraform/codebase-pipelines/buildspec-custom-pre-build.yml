version: 0.2

phases:
  build:
    commands:
    - >
        if [ -f ./custom-build/pre-build.sh ]; then 
          bash ./custom-build/pre-build.sh; 
        fi

  post_build:
    commands:
    - echo "Done"
