Once in the DBT PaaS conduit session

\! cf login -a https://api.london.cloud.service.gov.uk -u <your_email_address> --sso -o <govuk_pass_organisation> -s <govuk_paas_space>

To dump the data into the DBT PaaS conduit container...

\! cf conduit <postgres_backing_service> -- pg_dump --file dump.sql --data-only

Next up:

* Wipe DBT PaaS data
* Import data from file to DBT Paas
* Hopefully pipe data straight through from GOV.UK PaaS into DBT PaaS
